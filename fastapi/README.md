# Poetry Scripts

```toml
[tool.poetry.scripts]                                                           
server = "server:run"
export = "server:export_requirements"
create_api_user = "server:create_api_user"
get_api_user_token = "server:get_api_user_token"
```